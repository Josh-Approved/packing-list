# GENERATED by scripts/qa/compile-flow.mjs — DO NOT EDIT.
# Source of truth: qa/journey.json + qa/selectors.json.
# Re-generate:  node scripts/qa/compile-flow.mjs
# Validate:     node scripts/qa/lint-flows.mjs   (fails if this file is stale)
#
# STORE is supplied per-run via --env=STORE=<store>. There is deliberately NO
# flow-level env: block — a baked default out-ranks the CLI flag and writes
# captures to the wrong store dir (hard-won; see runbooks/qa-capture.md).

appId: com.joshapproved.packinglist
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: { text: "Greece.*" }
    timeout: 30000
- tapOn: { text: "Greece.*" }
- extendedWaitUntil:
    visible: { text: "Packers" }
    timeout: 10000
- assertNotVisible: { text: ".*Compass.*" }
- tapOn: { text: "New item name" }
- inputText: "Compass"
- pressKey: Enter
- assertVisible: { text: ".*Compass.*" }
- waitForAnimationToEnd:
    timeout: 1500
# killApp: real process death on Android; stopApp alias on iOS
# (no true force-kill via the iOS XCTest driver). See runbooks/testing-net.md § chaos net.
- runFlow:
    when:
      platform: Android
    commands:
      - killApp
- runFlow:
    when:
      platform: iOS
    commands:
      - stopApp
- launchApp:
    clearState: false
- extendedWaitUntil:
    visible: { text: "Greece.*" }
    timeout: 30000
- tapOn: { text: "Greece.*" }
- extendedWaitUntil:
    visible: { text: "Packers" }
    timeout: 10000
- assertVisible: { text: ".*Compass.*" }
